import { PREFIX } from '../constants';

export const TABLE_NAME = 'products';
export const ID_NAME = 'product_id';

export const PATH = `${PREFIX}/${TABLE_NAME}`;
export const RETRIEVE_PATH = `${PREFIX}/${TABLE_NAME}/:id`;
